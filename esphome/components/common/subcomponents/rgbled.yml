switch:
  - platform: gpio
    pin: ${RED_GPIO}
    id: LEDRed${INSTANCE}
    name: "Red ${INSTANCE}"
  - platform: gpio
    pin: ${GREEN_GPIO}
    id: LEDGreen${INSTANCE}
    name: "Green ${INSTANCE}"
  - platform: gpio
    pin: ${BLUE_GPIO}
    id: LEDBlue${INSTANCE}
    name: "Blue ${INSTANCE}"